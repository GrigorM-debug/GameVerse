@* _HeroSection.cshtml *@
<div class="hero-section">
    @* <video autoplay loop muted playsinline class="background-video">
        <source src="~/videos/HomeHeroSectionVideo.mp4" type="video/mp4">
        Your browser does not support the video tag.
    </video> *@
    
    <img src="images/heroImage2.jpg" class="background-video"/>

    <div class="overlay"></div>

    <div class="hero-content">
        <div>
            <h2>Welcome to The World of Gaming</h2>
            <p>Some short info about the site</p>
            <a asp-controller="GameStore" asp-action="Index" class="btn btn-primary btn-lg mt-4">Explore Our Game Store</a>
            <a asp-controller="Event" asp-action="Index" class="btn btn-primary btn-lg mt-4">Explore Our Events</a>
        </div>
    </div>
</div>

<!-- Add CSS styling -->
@* <style>
    .hero-section {
        position: relative;
        height: 50vh;
        overflow: hidden;
        width: 100%;
    }

    /* Video as background */
    .background-video {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        object-fit: cover;
        z-index: -1;
    }

    .hero-content {
        position: relative;
        z-index: 1;
        color: white;
        text-align: center;
        display: flex;
        justify-content: center;
        align-items: center;
        width: 100%;
        height: 100%;
    }

    .hero-content h1 {
        font-size: 4rem;
        font-weight: bold;
    }

    .hero-content p {
        font-size: 1.5rem;
    }

    .overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.5); /* Dark overlay for text readability */
        z-index: 0;
    }
</style> *@